name: Deployment
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  run-build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ls -a

      - run:
          npm run build

      - run:
          npm run deploy
